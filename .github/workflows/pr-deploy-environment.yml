# Deploys a temporary environment for testing a version of the code when a pull request is created / updated with a 'deploy' label
name: Deploy PR Environment
concurrency: deploy

on:
  pull_request:
    types: [ labeled, synchronize ]

jobs:
  deploy-pr-environment:
    if: contains(github.event.pull_request.labels.*.name, 'deploy')
    runs-on: ubuntu-latest
    steps:
      - env:
          EVENT_CONTEXT: ${{ toJSON(github.event) }}
        run: echo $EVENT_CONTEXT
      - env:
          GITHUB_BRANCH: ${{ github.event.pull_request.head.ref }}
          GITHUB_ACTION: ${{ github.event.action }}
        run: echo "Deploying branch '${GITHUB_BRANCH}' (pull request ${GITHUB_ACTION})!"

